# rwamp-feature-reflection — Requirements

## Module Timeline

- **Phase**: 2 (Discovery)
- **Tests**: 7
- **Package**: `ssg.rwamp.feature.reflection`

---

## Requirements

1. Implement `wamp.reflection.procedure.list` — list registered procedures
2. Implement `wamp.reflection.procedure.describe` — describe procedure details
3. Implement `wamp.reflection.topic.list` — list topics (exact, prefix, wildcard)
4. Implement `wamp.reflection.topic.describe` — describe topic details
5. Implement `wamp.reflection.type.list` and `type.describe` — type definitions
6. Implement `wamp.reflect.define` — define new types/errors
7. Implement `wamp.reflection.error.list` — list error definitions

---

**Last Updated**: 2026-08-14
